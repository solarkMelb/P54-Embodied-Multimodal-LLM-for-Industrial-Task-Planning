# LLM Backend
openai>=1.0.0
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-google-genai>=1.0.0
langchain-deepseek>=0.1.0
langchain-core>=0.2.0
langchain-ollama>=0.1.0

#Note: Torch is a dependency of ultralytics (YOLO), so you might need to uninstall the existing version and install the compatible one
#       pip uninstall torch torchvision torchaudio -y
#
#   CUDA 12.1 (most common):
#       pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
#   CUDA 11.8:
#       pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#   CPU only (no GPU):
#       pip install torch torchvision torchaudio
#
# Best practice is to manually install the compatible torch version first, then install the rest of the dependencies, to avoid version conflicts.
#       pip install -r requirements.txt


# Computer Vision
opencv-python>=4.8.0
ultralytics>=8.0.0
numpy>=1.24.0
Pillow>=10.0.0

# Simulation
pybullet>=3.2.5
pygame>=2.5.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Testing
pytest>=7.4.0

# PDF generation — helper_scripts/generate_Report.py only
reportlab>=4.0.0
